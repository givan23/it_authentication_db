-- Crea il database se non esiste già
CREATE DATABASE IF NOT EXISTS it_authentication
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;

-- Seleziona il database per le operazioni successive
USE it_authentication;